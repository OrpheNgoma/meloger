{% extends 'layouts/admin.html.twig' %}

{% block title %}Editer une Fonction
{% endblock %}

{% block body %}
<main class="pt-5 mx-lg-5">
  <div class="container-fluid mt-5">
		<h1>Editer la Fonction</h1>

		{{ include('admin/fonction/_form.html.twig', {'button_label': 'Modifier'}) }}

		<div align="right">
			<a href="{{ path('fonction_index') }}" class="btn btn-secondary">Retour à la liste</a>
		</div>

		{{ include('admin/fonction/_delete_form.html.twig') }}
	</div>
</main>
{% endblock %}
