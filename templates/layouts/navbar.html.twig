<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark fixed-top scrolling-navbar primary-color-dark">
	<a class="navbar-brand" href="{{path('home')}}">ME LOGER</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item {% if current_menu is defined and current_menu == 'home' %}active{% endif %}">
				<a href="{{ path('home') }}" class="nav-link">ACCUEIL</a>
			</li>
			<li class="nav-item {% if current_menu is defined and current_menu == 'properties' %}active{% endif %}">
				<a href="{{ path('app_property_index') }}" class="nav-link">LES PROPRIÉTÉS</a>
			</li>
		</ul>
		{% if app.user %}
			<ul class="navbar-nav">
				{% if app.user %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink-4" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							<i class="fas fa-user"></i>
							{{ app.user.username }}
						</a>
						<div class="dropdown-menu dropdown-menu-right dropdown-info" aria-labelledby="navbarDropdownMenuLink-4">
							<a class="dropdown-item" href="{{ path('property_index') }}">Mon compte</a>
							<a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a>
						</div>
					</li>
				{% endif %}
			</ul>
		{% endif %}

		{% if not app.user %}
			<ul class="navbar-nav">
				<li class="nav-item">
					<a href="{{ path('app_login') }}" class="nav-link">CONNEXION</a>
				</li>
			</ul>
			<ul class="navbar-nav">
				<li class="nav-item">
					<a href="{{ path('app_register') }}" class="nav-link">INSCRIPTION</a>
				</li>
			</ul>
		{% endif %}
	</div>
</nav>
<!-- Navbar -->